apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canso-fraud-analyst-db-foundation.fullname" . }}-test-connection"
  labels:
    {{- include "canso-fraud-analyst-db-foundation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canso-fraud-analyst-db-foundation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
