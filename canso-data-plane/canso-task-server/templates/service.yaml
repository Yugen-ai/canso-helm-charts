apiVersion: v1
kind: Service
metadata:
  name: {{ include "task-server.fullname" . }}
  labels:
    {{- include "task-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "task-server.selectorLabels" . | nindent 4 }}

